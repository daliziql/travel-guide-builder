---
name: travel-guide-builder
description: 旅行攻略工程化生产与迭代。结合内网旅游群/同事攻略文档（红榜经验与避坑）与公开渠道（小红书模拟人工浏览采集实拍与口碑、公开搜索核验事实），完成需求澄清、双源调研、逐日行程排程（交通/住宿/餐饮/时间轴/多方案），产出 Markdown 攻略与单文件自包含 HTML 行程图册（Leaflet 航图与逐日小地图、时间轴编号与地图对应、实拍图集点击弹窗、航段总览、红黑榜、备选方案、重点项目专栏、风险应急、启程倒计时），住宿通过授权 MCP（Trivago/Airbnb 等）采集报价与高评民宿、产出只读的酒店/民宿选型页，并支持版本备份与扁平化全英文 URL 的 GitHub Pages 发布。当用户要做旅游/出行/自由行行程规划、旅行攻略文档或 HTML 行程图册、沉淀同事旅游红黑经验、采集小红书旅行实拍/评论、或迭代已有 travel-books 类行程工程时使用。
---

# Travel Guide Builder · 旅行攻略工程化生产

把一次出行从「需求 → 双源调研 → 排程 → 图文成稿 → 验证 → 发布」做成可复用流水线。
核心差异点：**内网同事红黑经验打底 + 小红书强关联实拍 + 单文件 HTML「粗中有细」图册**。

## 0. 工程结构（长期稳定，先搭骨架再填内容）

**本地工程**用 trips 机制承载多份攻略；**对外发布仓**扁平化为全英文 URL（两者不同，别混）：

```
<本地工程根>/
├── shared/                     # 跨行程复用：workflow.md / sources.md / templates/ / extraction/
├── tools/                      # 跨行程脚本（图库构建、采集辅助），参数化、不写死行程
└── trips/<YYYY-MM-目的地>/
    ├── 00-需求与约束.md         # 阶段0产物
    ├── raw/                    # 原始素材（内网摘录、xhs 采集），保留出处，不发布
    ├── notes/                  # 提炼笔记（红黑候选、口碑统计）
    ├── lodging-data/           # 住宿报价 quotes_summary.json + 口碑 xhs_reputation.json
    ├── 行程攻略.md              # Markdown 成稿
    ├── 小红书口碑调研报告.md     # 样本量/频次/评论共识（见 references/01）
    ├── assets/<gallery>/       # 精选实拍（每点位一个子目录）+ gallery-data.js
    └── 行程攻略.html            # 单文件图册（相对路径引用 assets，可直接 file:// 打开）

<发布仓根>/                       # GitHub Pages：扁平、全英文、无中文、无 trips 嵌套
├── index.html                  # 主攻略本身（不做 meta-refresh 中转）
├── hotels/index.html           # 住宿只读选型页，URL 即 /<repo>/hotels/
└── assets/ lodging-data/ …     # raw/_cand/backup 不入库
```

新行程第一步：建目录、写 `00-需求与约束.md`；`raw/` 只增不改、可追溯。发布时再把成品扁平化为右侧结构（命令见 references/04）。

## 1. 标准流程（七阶段）

### 阶段 0 · 需求澄清（先问清再动手）
必须锁定：出行人（人数/体力/特殊需求）、日期与硬时刻（起降时间、请假窗口）、目的地与天数分配、
作息偏好（默认起床/出发时间，特种兵天数上限）、兴趣与必去项、预算档、交通住宿偏好、
去程/回程是否异地、硬禁忌。模糊项写「待确认」，**不编默认值**；用户后续补充信息时增量更新需求文件。

### 阶段 1 · 双源情报采集（产出 raw/ + 口碑调研报告）
- **内网优先（红黑经验来源，仅 ByteDance 内网用户）**：先判断是否为字节内网环境（企业身份/内网工具是否可用）；
  **非内网用户整段跳过内网采集**，不调用企业工具、不等权限、不视为阻塞，红黑经验改由公开游记/评论提炼，并注明「无内网来源」。
  内网用户再走：enterprise_agentic_search 用自然语言整问（行程/吃住/避坑/文档一次合并）；
  需要精确群聊时走飞书 IM skill（定位旅游群→时间线→关键词搜消息）；攻略文档用 lark-doc/lark-wiki 读全文。
- **公开核验**：general_search/web.fetch 核验签证政策、时区、营业时间、班期、票价、天气窗口；
  近年政策变更必须查到最新官方来源（使馆/总领馆/政府/航司），旧攻略可能过时。
- **小红书实拍与口碑**：必须用 browser-use 系 skill **模拟人工点击**浏览（搜索→点帖→慢速滚动→读正文+评论），
  禁止接口爬虫/直拉 CDN；样本量要够（扫描数百篇量级、深读数十篇+评论上千条），优先目标季节、
  同时补充季节无关样本。完整 SOP、防风控与 AI 双审门槛见 `references/01-intel-collection.md`。
- **住宿报价/民宿优先用授权 MCP**（不碰网页风控）：Trivago 官方远程 MCP 聚合酒店报价为主、Airbnb 社区 MCP 补高评民宿
  （软限流 25-30s）、Trip 官方 MCP 需 key（缺则浏览器人工补）、Booking 无 MCP 走 Trivago、小红书无 MCP 只能模拟人工。
  通道细节、防串店与只读选型页见 `references/05-lodging-acquisition.md`。

### 阶段 2 · 红黑经验提炼（产出 notes/）
按 行前/交通/住宿/餐饮/景点/购物/安全 分类；每条记：内容、红或黑、来源（谁/哪个群或文档/链接/时间）、适用条件。
多人交叉验证标「多人验证」，孤证标「单源待核」并用公开信息核验。黑榜必须写「坑是什么 + 怎么绕」。

### 阶段 3 · 排程（先骨架后细节，产出 行程攻略.md）
方法论（航段倒推、不连续飞行、体力曲线、作息约束、班期按星期核验、同酒店连住寄存、多方案）
见 `references/02-itinerary-method.md`。关键数字（时间/距离/价格）必须来自素材或可复现计算，距离用工具算，不口算。

### 阶段 4 · 实拍图库（双 AI Gate，产出 assets/）
按「点位关键词专搜 → Gate-1 审帖强相关 → Gate-2 逐张审图强关联 → 入库」执行，
每点位目标 10 张、不足如实告知，**严禁跨点位挪用、泛菜品/泛地貌凑数、人脸大头/视频截图**。
判据、目录约定、构建脚本用法见 `references/01` 第 3 节与 `scripts/build_gallery.py`。

### 阶段 5 · HTML 图册（单文件、粗中有细）
组件契约、DAYS 数据 schema、样式与文案规范见 `references/03-html-contract.md`，
起步骨架直接复制 `assets/html-scaffold.html`。紧凑主视图 + 点击弹窗看细节（实拍/链接/专栏）；默认简洁版、按钮切详细版。
需要订房决策时另建**只读**住宿选型页（酒店/民宿两组），采集通道与页面契约见 `references/05-lodging-acquisition.md`，样板 `assets/hotel-picker.html`。

### 阶段 6 · 验证（每轮必做，不过不交付）
在用户实际查看的浏览器 tab 上：重载页面 → 读 DOM/数据校验（每日事件、连线、图集键全部命中、0 console error）
→ 关键区域截图目检（对齐/图文匹配/不重复套图）→ 线上发布后 curl 关键资源 + 浏览器实测。清单见 `references/04-verify-release.md`。

### 阶段 7 · 备份与发布
- **版本快照**：大改动前存 `主攻略_vN备份.html`，并把其引用的图库物理复制一份 `assets/<gallery>_vN/`
  并 sed 改写内部路径，做到快照自包含；多版本最终归到 `backup/` 打包留档、不进 git。
- **GitHub Pages（扁平 + 全英文 URL）**：.gitignore 排除 raw/backup/_cand/.DS_Store；发布仓主攻略直接当根 `index.html`
  （不做中转跳转）、住宿页放单一英文子目录 `hotels/index.html`，URL 不含中文、不含 trips 段；`git mv` 保留历史、
  互链改相对英文路径；`gh repo create/rename` + Pages API；发布后 curl 根/hotels/资源 + 浏览器实测。完整命令见 `references/04-verify-release.md`。
- 出行后复盘：实际 vs 计划，新红黑回注 notes 与 shared/workflow.md 迭代记录。

## 2. 长期硬约束（来自真实迭代，默认遵守，用户另有指示时以最新指示为准）

1. **作息**：默认按用户起床/出发时间排（如 9-10 点起、10 点后出发）；全程只允许 1-2 天「特种兵」早起，并显式标注。
2. **不连续飞行**：两个飞行日之间隔完整游玩日；红眼/早班优先找同时段更晚替代班（查全天班期表，按星期几核验，标注「出票复核」）。
3. **体力曲线**：费腿/高强度项目前置、轻松休整后置；相邻高强度日必须提示并给对调方案。
4. **图片强关联**：餐厅图须见本店门脸/店名/同帖指名，景点图须能确认是该点；同一点位不重复套图，
   相邻行程在同一地点时只给目的地那一条配实拍；无图就留空，不硬凑。
5. **小红书采集铁律**：模拟人工点击、慢速、后台运行不抢用户前台、登录/验证码必须 interaction.request_action 交用户接管；
   评论区是重要信息源必须读；新关键词采集单独目录，避免与旧图串用。
6. **事实可追溯**：数字有来源、口碑有样本量（「N 篇提及」），外部事实与内网经验分开标注；
   应急电话/使馆信息标注核验日期并提醒出行前复核。
7. **文案风格**：调侃诙谐、不说教；倒计时等情绪文案先给候选让用户过一遍再应用。
8. **样式**：简洁统一、能对齐都对齐（固定列宽而非空格凑）；英文标签不混排导致溢出；每轮改完截图自检。
9. **改完即验**：file:// 不随磁盘改动自动刷新，必须在用户 tab 重新 navigate 后再截图判断。
10. **按需确认环境**：删除/覆盖类操作如实执行、被拒不换写法绕；备份与原始数据不擅自删除。
11. **发布 URL**：全英文、扁平（主攻略根 `index.html`、住宿页单一 `hotels/` 子目录），不暴露中文文件名与 trips 嵌套；本地 trips 机制与发布结构分开。
12. **住宿采集 MCP 优先**：授权 MCP（Trivago/Airbnb）能拿就不碰网页；小红书无 MCP 才模拟人工；报价以 quotes_summary.json 为唯一权威并带采集日期，不编深链/hotelId。
13. **住宿页只读**：只展示、分酒店/民宿两组，不放编辑/勾选对比/导出，内部采集 SOP 块不进发布页；民宿要评价好的，图片按全名强关联、防串店、无图留空。
14. **内网门禁**：内网采集只对 ByteDance 内网用户开放；非内网用户直接跳过全部企业内部工具，只用公开渠道，红黑经验从公开样本提炼并标注来源类型，判断不确定就先问用户是否在字节内网。

## 3. references / assets / scripts 索引（按需读取，勿一次性全读）

- `references/01-intel-collection.md`：内网红黑采集 + 小红书模拟人工 SOP + 双 AI Gate 图库管线 + 口碑报告口径。
- `references/02-itinerary-method.md`：排程方法论（航段倒推/班期核验/体力与作息/多目的地结构/多方案/红黑嵌入）。
- `references/03-html-contract.md`：单文件 HTML 组件清单、DAYS/arcs/GALLERY 数据契约、样式与弹窗规范。
- `references/04-verify-release.md`：浏览器验证清单、vN 快照做法、扁平英文结构的 git/GitHub Pages 发布命令与回滚。
- `references/05-lodging-acquisition.md`：住宿 MCP 采集通道（Trivago/Airbnb/Trip/Booking/小红书）、防串店、只读住宿选型页契约。
- `assets/html-scaffold.html`：图册起步骨架（复制后填数据）。
- `assets/hotel-picker.html`：只读住宿选型页样板（酒店/民宿两组，复制为 `hotels/index.html` 后填数据）。
- `scripts/build_gallery.py`：通用图库构建（精选清单 → 复制图片 + provenance.json + gallery-data.js）。
